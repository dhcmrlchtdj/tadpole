(dirs src)

(include_subdirs unqualified)

(library
 (name nonsense)
 (libraries containers)
 (preprocess
  (pps ppx_deriving.std))
 (flags
  (:standard -warn-error -a -w -30)))

; (executables
;  (names wat wasm eval)
;  (libraries containers)
;  (preprocess
;   (pps ppx_deriving.std)))
