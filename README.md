# tadpole

## usage

```
$ make build

$ wasm -wasm /path/to/wasm # wasm2wasm (strip custom sections)
$ wasm -wat /path/to/wasm # wasm2wat
$ wasm -i /path/to/wasm # internal

$ wat -wasm /path/to/wat # wat2wasm
$ wat -wat /path/to/wat # wat2wat (pretty print)
$ wat -i /path/to/wat # internal
```
